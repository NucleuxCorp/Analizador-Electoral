"""Single source of truth for the project version.

Bump this on every meaningful change so the running build is identifiable in
the UI (and after deploys / template-cache surprises).
"""

__version__ = "0.12.1"
